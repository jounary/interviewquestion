## 1、网络模型
七层网络结构：物理层、数据链路层、网络层（IP）、传输层（TCP，UDP）、会话层、表示层、应用层（HTTP，HTTPS，SMTP，DNS）。

### 1.1 rpc与http的区别？优点及缺点？
1. 首先从七层网络结构上来说，http属于应用层，rpc的速度比http要块，虽然这两者都基于tcp协议，但是http协议的信息比较臃肿。  
2. 从实现层度上来说，http不需要关心实现细节，可以实现跨平台，跨语言，而rpc的约束较多。  
3. 从实现的框架上来看，基于http开发实现的是RESTful风格的接口，返回得到JSON或者XML文档，然后在客户端进行解析，使用简单。而rpc框架一般都有注册中心，实现丰富的监控管理。

### 1.2 http1.x与http2协议的区别及特性？
1. http基于文本格式，而http2基于二进制传输。  
2. http2具有多路复用的特性，同域名下的通信，都在一个TCP连接上完成，消除了TCP连接带来的延时和内存消耗。  
3. http2进行了头部压缩处理，减少冗余的信息。  
4. http2具有主动推送HTML页面资源的能力，不需要等到浏览器解析到相应位置再发起请求。  

总结：HTTP/2基于二进制传输，通过支持请求与响应的多路复用来减少延迟，通过压缩HTTP首部字段将协议开销降至最低，同时增加对请求优先级和服务器端推送的支持。

### 1.3 http与https的区别？
1. HTTP是一种广泛应用的网络协议，而HTTPS是完全的HTTP协议，HTTPS的安全是基于SSL的。
2. http是超文本传输协议，信息是明文传输，https则是具有安全性的ssl加密传输协议。
3. http和https使用的是完全不同的连接方式，用的端口也不一样，前者是80，后者是443。


## 2、TCP的三次握手与四次握手流程。
