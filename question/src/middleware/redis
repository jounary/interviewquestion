### 1.为什么要用redis做缓存？
高性能：
高并发：



### 面经
1.redis有序集合zset的底层实现？
zset底层使用ziplist和skiplist两种方式实现，当元素个数小于128个或者元素的大小都小于64字节则会使用ziplist作为底层存储结构。这是一个为了节省空间的数据结构。
  
跳表如何插入？

zet的使用场景：
1.延时队列？
2.限流
3.排行榜

2.手写一个redis分布式锁
3. redis哈希表实现，扩容以及宕机恢复数据。