## java
## go

#### 1.java与go语言的区别？各有什么优势？
首先冲语言角度来说，Java与go都是编译型语言，但是Java编程生成.class文件，需要在jvm上运行，而go直接编译生成二进制文件。并且GO不是一种OOP语言，不支持Java中的继承和多态的特性。
其次从数据类型上来说，go语言支持指针，而Java不支持，go语言有一个切片相当于java中字符串的分割。

在优势上比较，首先要说的就是性能，go语言速度快，并且天然支持并发的优势。go语言支持protocol buffers 和grpc，快速构建微服务。但是go语言没有Java语言完整的体系，缺少框架，缺少对包的管理。


